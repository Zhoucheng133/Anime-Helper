let prels=[];
let ls=[];

export const setPrels=(val)=>{
  prels=val;
}

export const setLs=(val)=>{
  ls=val;
}

export const getPrels=()=>{
  return prels;
}

export const getLs=()=>{
  return ls;
}